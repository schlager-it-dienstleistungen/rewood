export class User {
		id: number;
		uid: string; // Firebase User-UID
		username: string;
		// password: string;
		email: string;
		roles: number[];
		fullname: string;
		companyName: string;
		phone: string;
		emailVerified: boolean;
}
